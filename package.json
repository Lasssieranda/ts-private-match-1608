{
  "name": "tiefstapel-private-online",
  "version": "0.3.8",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/online-controller.js --bundle --format=esm --minify --outfile=src/online.bundle.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/engine.js && node --check src/app.js && node --check src/online-controller.js && node --check src/online.bundle.js && node --check sw.js",
    "serve": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "@trystero-p2p/mqtt": "^0.25.3",
    "esbuild": "^0.25.8",
    "playwright": "^1.62.1"
  }
}
